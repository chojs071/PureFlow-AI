# PureFlow AI — MVP

## 1. MVP 목적
> **공정 품질을 유지하면서 기존 초순수 사용량에서 얼마나 줄일 수 있는지를 사용자가 직접 확인하게 한다.**

핵심 가치가 짧은 데모 안에 이해되도록 구현한다.

## 2. 필수 기능
### F-01. 웨이퍼 선택
- 200mm
- 300mm

### F-02. AI 최적화 시작
버튼: **AI 최적화 시작**

분석 애니메이션:
```text
웨이퍼 상태 분석
↓
오염도 분석
↓
세정 조건 분석
↓
UPW 절감 가능량 계산
```

### F-03. 공정별 단계 이동
공정은 한 번에 하나씩 보여준다.

### F-04. 공정별 오염도
0~100 게이지로 표시한다.

### F-05. 기준 세정 조건
현재 공정의 기준 세정시간, 린스시간, 유량 등을 표시한다.
실제 장비 레시피가 아니라 PureFlow AI 기준 시뮬레이션 데이터로 관리한다.

### F-06. 기존 UPW
현재 조건에서 계산된 기존 UPW 사용량 표시.

### F-07. AI 추천 UPW
품질 기준을 만족하는 후보 중 최소 UPW 조건을 추천.

### F-08. 절감량
`기존 UPW - AI UPW`

### F-09. 절감률
`절감량 / 기존 UPW × 100`

### F-10. 품질 점수
AI 추천 조건의 품질 점수를 0~100으로 표시.

### F-11. 다음 공정
분석 완료 후 다음 공정 버튼 활성화. 마지막에는 최종 결과 보기.

## 3. MVP AI 로직
실제 머신러닝 모델을 반드시 사용할 필요는 없다. MVP는 **규칙 기반 최적화 엔진**으로 AI 의사결정 과정을 구현한다.

```text
입력
↓
웨이퍼 크기 / 공정 / 오염도 / 기준 세정 조건
↓
후보 조건 생성
↓
품질 점수 계산
↓
품질 기준 미달 후보 제거
↓
UPW 사용량 비교
↓
최소 UPW 후보 선택
↓
AI 추천
```

## 4. 후보 조건
기준 조건에서 허용된 범위 안의 여러 후보를 생성한다.

예:
```text
10분 / 8분 / 10L
9분 / 8분 / 10L
9분 / 7분 / 10L
8분 / 7분 / 9L
```

## 5. 품질 점수
MVP에서는 시뮬레이션 함수로 계산한다.

개념:
```text
품질 점수
= 기본 품질
- 세정시간 감소 패널티
- 린스 감소 패널티
- 유량 감소 패널티
- 오염도 보정
```

0~100으로 제한한다.

## 6. 품질 기준
기본 기준:
```text
qualityScore >= 95
```
기준 미달 후보는 제외한다.

## 7. 오염도 90 이상
```text
if contaminationScore >= 90:
    minimumCleaningTime 유지
```
최소 세정시간을 줄이지 않는다. 다른 변수는 허용 범위에서 최적화할 수 있다.

## 8. UPW 계산
```typescript
function calculateUPW(flowRate, time, cycles) {
  return flowRate * time * cycles;
}
```

복수 단계:
```typescript
totalUPW = steps.reduce(
  (sum, step) => sum + step.flowRate * step.time * step.cycles,
  0
);
```

## 9. 최종 결과
```text
총 기존 UPW
= 모든 공정 기존 UPW 합계

총 AI UPW
= 모든 공정 AI UPW 합계

총 절감량
= 총 기존 UPW - 총 AI UPW

총 절감률
= 총 절감량 / 총 기존 UPW × 100
```

## 10. MVP 화면
### 시작
```text
PureFlow AI
AI 기반 초순수 최적화

웨이퍼 선택
[200mm] [300mm]

[AI 최적화 시작]
```

### 공정 시뮬레이션
```text
① ② ③ [④] ⑤ ...

현재 공정
식각

오염도
85 / 100

기준 UPW
120 L

AI 추천
90 L

절감
30 L

품질
97 / 100

[다음 공정]
```

### 최종 결과
```text
AI 최적화 완료

기존 UPW
XXXX L

AI UPW
XXXX L

총 절감
XXXX L

절감률
XX.X%

[공정별 비교 그래프]

[처음으로]
```

## 11. MVP 제외
- 로그인/회원가입
- 실제 DB
- 실제 AI API
- 실제 장비 연결
- 실시간 센서
- 공정 자동 제어
- 관리자 페이지
- 사용자별 레시피 편집
- 생산라인 연동

## 12. 성공 조건
사용자가 3분 이내에 다음을 이해하면 성공이다.

> “공정마다 오염도가 다르고, 기존 세정 조건에서 품질을 유지하면서 줄일 수 있는 UPW를 AI가 판단하는구나.”

## 13. 핵심 문구
> **기존 초순수 사용량에서 어느 정도까지 줄여도 공정 품질을 유지할 수 있는지 판단합니다.**
